{
  "name": "vip-evento-bonus-functions",
  "description": "Firebase Cloud Functions para processar bônus Evento VIP no servidor.",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.4.0"
  }
}
